# Environment Configuration Example
# 
# IMPORTANT: Copy this file to create environment-specific configs:
#   - .env.local  → for local development (DEBUG=True)
#   - .env.prod   → for production deployment (DEBUG=False)
#
# The settings will automatically load the correct file based on:
#   - ENV variable (local/prod)
#   - DEBUG variable (True loads .env.local, False loads .env.prod)

# Environment selection (optional - auto-detected from DEBUG)
# ENV=local  # or 'prod'

# Debug mode (determines which .env file to load)
DEBUG=True

DJANGO_SETTINGS_MODULE=core.settings.dev
DJANGO_SECRET_KEY=change-me
DJANGO_ALLOWED_HOSTS=*
TIME_ZONE=UTC

# PostgreSQL - REQUIRED (no fallback, will raise error if missing)
POSTGRES_DB=halalchicken
POSTGRES_USER=postgres
POSTGRES_PASSWORD=xusniddin2004
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Redis/Celery
REDIS_URL=redis://localhost:6379/0
# Storage backend (LOCAL|S3|CLOUDINARY). Only LOCAL implemented now.
STORAGE_BACKEND=LOCAL
# Local has no extra required vars.

# For S3 backend
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_S3_BUCKET=...
# AWS_S3_REGION=...
# S3_BASE_PATH=uploads
# S3_PRESIGN_EXPIRES=900

# For Cloudinary backend
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
# CLOUDINARY_FOLDER=halalchicken

# Image validation
MAX_IMAGE_MB=5

# Optional throttle overrides (defaults in settings)
# DRF_THROTTLE_AUTH=10/min
# DRF_THROTTLE_ORDER_CREATE=5/min
# DRF_THROTTLE_UPLOAD_IMPORT=3/min


# JWT
JWT_ACCESS_MIN=60
JWT_REFRESH_DAYS=7


# CORS (prod)
CORS_ALLOWED_ORIGINS=
